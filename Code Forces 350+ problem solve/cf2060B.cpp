///In the name of Allah

#include <bits/stdc++.h>
using namespace std ;

void fahad()
{
    int n ,m ;
    cin >> n >> m;

    vector<int>v(m);

    while(n--)
    {
        for(int i = 0 ; i < m ;i++)
        {
            cin >> v[i];
        }
    }
}

int main ()
{
    int t;
    cin >> t;

    while(t--)
    {
        fahad();
    }
}
